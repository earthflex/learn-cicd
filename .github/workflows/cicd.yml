name: CI/CD Pipeline

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-lastest

        steps:
            - name: Say Hello
                run: echo "Github Action!"
